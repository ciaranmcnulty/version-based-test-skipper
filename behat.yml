default:
  suites:
    core:
      contexts: [ CoreContext ]
      paths:    [ %paths.base%/features/skipping ]
    behat-scenario:
      contexts: [ Behat\ScenarioContext ]
      paths:    [ %paths.base%/features/skipping ]
    behat-scenario-endtoend:
      contexts: [ Behat\EndToEndContext ]
      paths:    [ %paths.base%/features/skipping ]

